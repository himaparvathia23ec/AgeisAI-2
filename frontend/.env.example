# Backend API URL (used by Vite at build time).
# For local dev you can omit this (defaults to http://127.0.0.1:8000).
# For production set to your deployed backend URL, e.g. https://aegisai-api.onrender.com
VITE_API_URL="http://127.0.0.1:8000"
